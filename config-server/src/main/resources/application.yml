server:
  port: 8080

spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/Paz-Santangelo/ConfigServer-ProyectoFinal-Microservicios-TodoCode.git
          searchPaths: config-data
          username: ${GIT_USER}
          password: ${GIT_PASSWORD}